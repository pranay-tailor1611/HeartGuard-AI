# Python Version for Cloud Deployment
PYTHON_VERSION=3.11.9

# Server Configuration
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=production

# CORS Security (Comma-separated list of allowed origins or * for all)
ALLOWED_ORIGINS=*
